# 用户维护及使用手册（增量）

## 1. CLI 新能力

### 1.1 计划指纹

`plan-static` 输出 JSON 新增 `spec_fingerprint`，用于标识模型语义。

### 1.2 严格计划匹配

- `analyze-wcrt --plan-json <path>` 默认 strict
- `export-os-config --config <path> --plan-json <path>` 默认 strict（仅 `--allow-plan-mismatch` 可显式放行）

当计划文件与配置指纹不一致时，命令返回码为 2。

### 1.3 strict 默认脚手架模板（推荐）

新增脚本：`review/scripts/strict_plan_pipeline.sh`（默认强制 strict）。

```bash
bash review/scripts/strict_plan_pipeline.sh examples/at06_time_deterministic.yaml
```

可选输出目录：

```bash
bash review/scripts/strict_plan_pipeline.sh examples/at06_time_deterministic.yaml review/runtime/i2/strict_plan_pipeline
```

脚本固定流程：
1. `plan-static`
2. `analyze-wcrt --plan-json <path>`（默认 strict）
3. `export-os-config --config <path> --plan-json <path>`（默认 strict）

失败诊断入口：
- `review/runtime/i2/strict_plan_pipeline/results.tsv`
- `review/runtime/i2/strict_plan_pipeline/logs/*.log`

CI 门禁：
- `.github/workflows/ci.yml` 新增 `strict_plan_gate`（独立非性能门禁，固定运行 strict 模板）。

### 1.4 双口径可调度率

`benchmark-sched-rate` 输出：

- 严格：`candidate_only_schedulable_rate`、`candidate_only_uplift`
- 兼容：`best_candidate_schedulable_rate`、`uplift`

## 2. UI Planning 面板

位置：左侧编辑区 `Planning` 页签。

功能：

1. 配置 planner / task scope / LP objective / WCRT 参数。
2. 一键执行 `Plan Static`、`Analyze WCRT`、`Export OS Config`。
3. 通过 `Random Task Generator` 生成可复现任务集。
4. 查看 Schedule/WCRT/OS Config 三张结果表与 Planning Logs。

## 3. FR-13 Compare 面板

位置：右侧遥测区下半部分，默认折叠。

当前边界：

1. UI 现通过 ordered scenarios 列表构建对比：第 1 项是 baseline，第 2 项是 focus，第 3 项起继续扩展。
2. 面板支持添加 metrics 文件、添加最近一次运行、删除所选场景、上移/下移顺序、构建报告与导出 JSON / CSV / Markdown。
3. 构建后的 report JSON 继续复用既有 schema，2 场景输出 `two_way`，3+ 场景输出 `n_way`。
3. 导出支持 `JSON / CSV / Markdown` 三种格式；其中 Markdown 结构用于和研究报告侧保持统一阅读体验。

## 4. Research Report 导出

位置：主工具栏 `Export Research Report`。

导出行为：

1. 依赖最近一次 UI 成功运行缓存的 `spec/events`；未运行时按钮会提示先执行仿真。
2. 底层直接复用 `build_model_relations_report`、`build_audit_report`、`build_research_report_payload` 与 `render_research_report_markdown`，不维护第二套报告逻辑。
3. 一次导出会生成：
   - `research-report.md`
   - `research-report.json`
   - `research-report-summary.csv`
4. quality 快照优先复用 UI 缓存；若缓存为空，则回退读取 `artifacts/quality/quality-snapshot.json`。

## 5. 状态流展示

右侧遥测区新增状态流视图，展示：

- Released
- Ready
- Executing
- Blocked

## 6. 常见问题

1. 严格匹配失败：优先检查 `plan.json` 是否由当前 `--config` 重新生成。
2. `candidate_only_uplift` 低于 `uplift`：这是预期行为，说明兼容口径包含了 baseline 可调度样本。
3. 点击 `Export Research Report` 提示先运行：说明当前会话还没有缓存有效 `spec/events`，先执行一次完整仿真。
4. 导出的研究报告缺少 quality 细节：优先检查 `artifacts/quality/quality-snapshot.json` 是否存在或当前 UI 是否已注入缓存快照。
