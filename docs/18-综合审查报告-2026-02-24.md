# 综合审查报告（设计文档 / 开发文档 / 代码 / 完成度）

## 0. 文档控制
- 版本：v0.5
- 日期：2026-03-07
- 审查范围：仓库根目录当前主干（文档、代码、测试、CI、样例）
- 证据基线：`evidence_git_sha=2f6e8911c65170af321078bc8e22e7964f67812c`
- 工作区基线：`workspace_git_sha=2f6e8911c65170af321078bc8e22e7964f67812c`
- 本轮复核命令（已执行）：
  - `python -m pytest -q`
  - `python scripts/quality_snapshot.py --output artifacts/quality/quality-snapshot.json --coverage-json artifacts/quality/coverage.json`
  - `python -m rtos_sim.cli.main validate -c examples/at01_single_dag_single_core.yaml --strict-id-tokens`
  - `python -m rtos_sim.cli.main run -c examples/at01_single_dag_single_core.yaml --events-out /tmp/events.jsonl --metrics-out /tmp/metrics.json --audit-out /tmp/audit.json`
  - `for f in examples/at0{1..9}_*.yaml examples/at10_*.yaml; do python -m rtos_sim.cli.main inspect-model -c "$f" --strict-on-fail; done`
  - `python -m rtos_sim.cli.main batch-run -b examples/batch_matrix.yaml --strict-fail-on-error`

---

## 1. 执行摘要

✅ 结论：
- 未发现导致主流程不可用的 P0 阻断缺陷。
- 核心链路 `validate/run/inspect-model/batch-run` 可稳定运行，官方样例 `at01~at10` 在严格门禁下通过。
- 当前主要风险集中在 **交付基线冻结治理** 与 **UI 高级交互 backlog**，属于 P1/P2 治理项，不影响当前主链路交付性。
- Phase 4 当前已完成 Compare 报告/导出层研究化收口，并接入 UI `Export Research Report` 入口；未完成项已明确保留为 backlog。

📊 量化结果（2026-03-07）：
- 自动化测试：`424 passed`
- 总覆盖率：`89.29%`（`line_rate=89.29225137278829`）
- 需求追踪矩阵：`M-01 ~ M-12` 共 `12/12` 标记“已实现”（`docs/14-docx需求追踪矩阵.md`）
- 综合完成度：**92/100（绿）**
  - 功能完整度：95
  - 设计一致性：91
  - 工程质量：90
  - 文档与交付治理：86

---

## 2. 证据快照

### 2.1 质量与回归
- `pytest -q`：`424 passed`（本轮本地复核）。
- `quality_snapshot`：`status=pass`，`evidence_git_sha=2f6e8911c65170af321078bc8e22e7964f67812c`，`pytest.passed=424`，`coverage.line_rate=89.29225137278829`（`artifacts/quality/quality-snapshot.json`）。

### 2.2 核心命令链路抽检
- `validate`：返回 0，输出 `[OK] config validation passed`。
- `run`：返回 0，输出 `[OK] simulation completed, events=13, now=12.000`。
- `inspect-model`：`at01~at10` 严格模式全部返回 0。
- `batch-run --strict-fail-on-error`：`runs=4, success=4, failed=0`。
- `benchmark-sched-rate`：门禁固定为 `candidate_only_uplift >= 0.30`，冻结事实 `actual=1.75`（`review/runtime/i1/sched_rate_gate/summary.txt`）。

### 2.3 设计一致性抽检
- 架构分层与实现入口一致：`docs/08-综合架构设计.md:13`、`docs/09-概要设计.md:24`、`rtos_sim/core/engine.py:101`、`rtos_sim/cli/main.py:95`。
- 发布链路已补齐草稿发布流程：`.github/workflows/release-draft.yml:1`。

---

## 3. 发现清单（按严重级别）

### F-01（P1 / Verified）综合报告内部数字不一致
- 现象：同一份报告中曾出现历史测试通过数表述不一致，与事实 `424 passed` 不一致。
- 证据：`docs/20-审查问题台账.csv:10`（REV-009 历史记录），对照 `artifacts/quality/quality-snapshot.json`。
- 影响：评审结论可追溯性下降，文档可信度受损。
- 状态：**已修复**（本版统一为 `424 passed`）。

### F-02（P2 / Risk）UI 主文件体量过大，回归冲击面偏高
- 现象：`rtos_sim/ui/app.py` 单文件 `2138` 行，承担编辑、渲染、交互、状态同步等多职责。
- 证据：`rtos_sim/ui/app.py:1`。
- 影响：定位成本高，变更耦合度高，复杂交互迭代风险较大。
- 状态：**open**（已拆出 `gantt_helpers`，仍需继续分解）。

### F-03（P2 / Partial）UI 模块测试覆盖率低于主干平均值
- 现象：`rtos_sim/ui/app.py` 覆盖率约 `86.60%`，仍低于项目总体 `89.29%`。
- 证据：`artifacts/quality/coverage.json`（按文件统计）。
- 影响：交互边界变化时，回归漏检概率上升。
- 状态：**partial**（已补 Compare Markdown 导出、Research Report UI 导出与运行态缓存相关回归，复杂 DAG 交互边界仍需继续补测）。

### F-04（P3 / Verified）文档基线措辞清理（已完成）
- 现象：此前多份文档标题或章节仍使用“草案”表述。
- 处理：已将正文表述统一为“基线/归档”语义，避免评审口径歧义（文件历史命名保留不影响引用）。
- 证据：`docs/08-综合架构设计.md:86`、`docs/06-时序图与类图.md:1`、`docs/05-配置文件Schema草案.md:1`、`docs/03-术语与数据模型草案.md:1`、`docs/02-架构图草案与模块接口定义.md:1`、`docs/01-需求规格说明书提纲.md:5`。
- 状态：**resolved**。

### F-05（P1 / Verified）Wayland 下 Tooltip popup 警告风暴并可能触发 UI 崩溃（已修复）
- 现象：`rtos-sim ui -c examples/at01_single_dag_single_core.yaml` 在 Wayland 下悬停触发大量 `Failed to create popup`，并可能 `Segmentation fault`。
- 证据：`rtos_sim/ui/app.py:1919`（tooltip parent 修复）；`tests/test_ui_gantt.py:113`（回归）。
- 影响：UI 可用性下降，教学演示与交互调试存在中断风险。
- 状态：**resolved**（已改为 `QToolTip.showText(..., viewport)`）。

### F-06（P1 / Verified）UI 完成态重复消费已流式下发事件（2026-03-07 已修复）
- 现象：`SimulationWorker.finished_report` 发送完整 `all_events` 后，UI 完成态曾将整批事件再次喂回状态流，导致 Gantt/状态展示重复。
- 证据：`rtos_sim/ui/worker.py:128`、`rtos_sim/ui/controllers/run_controller.py:41`、`rtos_sim/ui/controllers/run_controller.py:153`、`tests/ui/test_run_controller.py:208`。
- 影响：结束态可视化可能出现重复回放，研究报告上下文也容易被误判为“尾批事件语义”。
- 状态：**resolved**（完成态现仅补未流式下发的尾批事件，完整 `all_events` 继续保留给 Research Report）。

---

## 4. 完成情况评估

### 4.1 已完成（可稳定交付）
- 核心仿真链路（事件、调度、协议、ETM、开销、指标）：`rtos_sim/core/engine.py:101`。
- CLI 全链路（validate/run/ui/batch-run/compare/inspect-model/migrate-config）：`rtos_sim/cli/main.py:387`。
- 研究闭环审计主链路（含 `compliance_profiles`）：`rtos_sim/analysis/audit.py:224`、`rtos_sim/analysis/audit.py:264`。
- 发布草稿流水线（tag 触发 + draft release）：`.github/workflows/release-draft.yml:1`。
- Wayland tooltip 兼容修复（避免 popup 警告风暴）：`rtos_sim/ui/app.py:1919`。

### 4.2 部分完成（可用但需治理）
- UI 模块职责拆分仍在进行中：`rtos_sim/ui/app.py:1`。
- 文档治理（基线化/术语一致性）仍需持续收口：`docs/20-审查问题台账.csv:8`。

### 4.3 未发现阻断项
- 本轮未发现会中断 `validate/run/inspect-model/batch-run` 主流程的 P0 缺陷。

---

## 5. P0~P3 整改优先级与验收口径

1. **P1（当周完成）**
   - 统一所有审查文档中的事实数字（测试数、覆盖率、SHA）引用来源为质量快照。
   - 验收：随机抽检 `docs/10/11/14/15/18/19` 与 `quality_snapshot` 一致。

2. **P2（下个迭代）**
   - UI 模块继续按职责拆分（配置编辑 / DAG 交互 / Gantt 渲染 / 结果面板）。
   - 补充 UI 关键交互回归（异常路径、拖拽连线边界、表格校验边界）。
   - 验收：`pytest -q` 通过，UI 子模块覆盖率提升并稳定在目标阈值以上。

3. **P3（治理项）**
   - 文档基线化持续治理：保持“基线/归档”语义一致，历史命名仅用于路径兼容。
   - 验收：`docs/` 面向交付的主文档不出现措辞歧义，并保持证据可追溯。

---

## 6. 本轮交付物
- 综合审查报告（本文件）：`docs/18-综合审查报告-2026-02-24.md`
- 用户使用说明书（补强版）：`docs/19-用户使用说明书.md`
- 审查问题台账（P0~P3）：`docs/20-审查问题台账.csv`

## 7. 收口更新（2026-02-27）

- 基线一致性自动化已落地：新增 `scripts/check_doc_baseline_consistency.py` 与 `tests/test_doc_baseline_consistency.py`，可对 `docs/10/11/14/15/18/19/20/21/22` 与 `artifacts/quality/quality-snapshot.json` 做字段一致性门禁。
- UI 可维护性拆分已推进：将 FR-13 Compare 面板的 metrics 读写与报告导出 I/O 下沉到 `rtos_sim/ui/compare_io.py`，主窗口 `app.py` 仅保留交互编排。
- 新增 UI 分层最小回归：`tests/test_ui_compare_io.py` 覆盖 compare I/O（JSON/CSV）路径，且 `tests/test_ui_gantt.py` 保持全绿。
- 本轮回归抽检（2026-02-27）：
  - `python -m pytest -q tests/test_ui_compare_io.py tests/test_ui_gantt.py tests/test_audit.py` 通过
  - `python -m pytest -q` 通过（`424 passed`）
  - `validate/run/inspect-model/batch-run/research_case_suite` 全部通过（严格参数口径）
  - `python scripts/check_doc_baseline_consistency.py --snapshot artifacts/quality/quality-snapshot.json --docs-root docs` 通过
- 闭环文档已补齐：`docs/22-分阶段验收报告.md`（记录 S1~S4 交付与门禁执行结果）。


## 8. S9 验证与交接（2026-03-01）

- 已完成全量回归：`python -m pytest -q`（`424 passed`）。
- 已锁定质量快照：`artifacts/quality/quality-snapshot.json`（`evidence_git_sha=2f6e8911c65170af321078bc8e22e7964f67812c`，`coverage=89.29%`）。
- 已完成 CLI 主链路抽检：`validate/run/inspect-model/batch-run` 全部通过。
- 已补齐交接产物：`artifacts/m01m12-governance-bcd-20260228/v1/verify-report.md`、`artifacts/m01m12-governance-bcd-20260228/v1/handoff.md`。


## 9. 收口更新（2026-03-07）

- 30% 可调度率验收口径已固定为 `candidate_only_uplift`，兼容口径 `best_candidate_schedulable_rate/uplift` 仅保留展示，不再参与结论判定。
- FR-13 Compare 已完成最小研究化收口：compare report JSON 新增 `comparison_mode`、`scenario_labels`、`scenarios`、`scalar_summary`、`core_utilization_summary`，UI 导出支持 `JSON / CSV / Markdown`。
- UI 已新增 `Export Research Report`，直接复用 `build_model_relations_report`、`build_audit_report`、`build_research_report_payload` 与 `render_research_report_markdown`，输出 `.md + .json + -summary.csv`。
- 当前未完成边界已保留：Compare UI 仍是 left/right 双路装载与构建；DAG 多选 / 批量移动 / 批量删除仍在 backlog，不将其表述为“本轮已完成”。
- 文档与交付口径已同步到 `docs/22`、`docs/26`、`review/02`、`review/06`。
