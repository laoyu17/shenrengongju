# RTOS 仿真工具用户使用说明书（新用户 / 测试同学）

## 0. 文档信息
- 版本：v0.4
- 日期：2026-03-04
- 适用版本：`rtos-sim 0.2.x`
- 适用对象：初次使用者、测试/验证同学、评审联调同学
- 证据基线：`evidence_git_sha=a98218562038b60c23a0b99ab630780c3da67970`
- 工作区基线：`workspace_git_sha=a98218562038b60c23a0b99ab630780c3da67970`
- 质量快照：`pytest=437 passed`，`coverage=89.37%`
- 事实源：`artifacts/quality/quality-snapshot.json`

---

## 1. 工具能做什么

`rtos-sim` 是一个配置驱动的 RTOS 异构多核调度仿真工具，支持：
- 配置合法性校验（Schema + 语义）
- 仿真运行与事件/指标导出
- 审计报告导出（协议一致性/研究闭环）
- 模型关系导出（任务/子任务/分段 与 核/资源）
- 批量实验（参数矩阵）
- 指标对比（CLI 输出 JSON + CSV；UI 额外支持 Markdown）
- 可选 PyQt UI 可视化（含 Compare / Research Report 导出入口）

---

## 2. 环境准备

### 2.1 基础环境
- Python：`>=3.10`
- 操作系统：Linux / Windows（CI 已覆盖）

### 2.2 安装命令

```bash
python -m pip install -e .[dev]
```

若需要 UI：

```bash
python -m pip install -e .[ui]
```

### 2.3 安装验证

```bash
rtos-sim --help
python -m pytest -q
```

预期：测试全通过（当前基线约 `437 passed`）。

---

## 3. 目录与样例说明

- 运行样例：`examples/at01~at10_*.yaml`
- 批跑样例：`examples/batch_matrix.yaml`
- 研究反例集：`examples/research_counterexamples.json`
- 主要输出目录建议：`artifacts/`

---

## 4. 5 分钟快速上手（推荐）

### Step 1：校验配置

```bash
rtos-sim validate -c examples/at01_single_dag_single_core.yaml
```

成功标志：输出 `[OK] config validation passed`。

### Step 2：运行仿真并导出产物

```bash
rtos-sim run -c examples/at01_single_dag_single_core.yaml \
  --events-out artifacts/events.jsonl \
  --metrics-out artifacts/metrics.json \
  --audit-out artifacts/audit.json
```

成功标志：输出 `[OK] simulation completed ...`。

### Step 3：导出模型关系并做严格门禁

```bash
rtos-sim inspect-model -c examples/at01_single_dag_single_core.yaml \
  --out-json artifacts/model_relations.json \
  --out-csv artifacts/model_relations.csv \
  --strict-on-fail
```

成功标志：输出 `status=pass`，退出码为 `0`。

---

## 5. CLI 命令详解

### 5.1 `validate`（配置校验）

用途：检查配置文件结构与语义是否合法。

```bash
rtos-sim validate -c <config.yaml>
```

严格 ID 分隔符模式（建议 CI 用）：

```bash
rtos-sim validate -c <config.yaml> --strict-id-tokens
```

注意：`examples/batch_matrix.yaml` 属于批跑配置，不可用 `validate`。

---

### 5.2 `run`（运行仿真）

用途：执行仿真并输出事件、指标、审计报告。

```bash
rtos-sim run -c <config.yaml> \
  --events-out artifacts/events.jsonl \
  --metrics-out artifacts/metrics.json \
  --audit-out artifacts/audit.json
```

常用可选参数：
- `--until <float>`：覆盖仿真时长
- `--step --delta <float>`：步进执行
- `--pause-at <float>`：在指定仿真时间暂停并导出部分结果
- `--events-csv-out <path>`：导出事件 CSV（用于表格分析）

失败判定：
- 配置错误返回 `1`
- 审计失败（`--audit-out` 且 audit 非 pass）返回 `2`

---

### 5.3 `inspect-model`（模型关系导出）

用途：导出任务/子任务/分段与核/资源关系，并给出语义判定。

```bash
rtos-sim inspect-model -c <config.yaml> \
  --out-json artifacts/model_relations.json \
  --out-csv artifacts/model_relations.csv
```

严格门禁模式：

```bash
rtos-sim inspect-model -c <config.yaml> --strict-on-fail
```

失败判定：`status != pass` 时返回 `2`（仅 strict 模式）。

---

### 5.4 `batch-run`（批量实验）

用途：按矩阵展开多组实验并汇总。

```bash
rtos-sim batch-run -b examples/batch_matrix.yaml
```

常用可选参数：
- `--output-dir <dir>`：指定批跑输出目录
- `--summary-csv <path>`：指定汇总 CSV 路径
- `--summary-json <path>`：指定汇总 JSON 路径

目录边界规则（2026-03-05 更新）：
- 批配置文件中的 `output_dir` 必须落在批配置所在目录内，防止 `../` 越界写文件。
- 如确需输出到工作区外，请使用 CLI 显式参数 `--output-dir <absolute-or-relative-path>`。

失败可观测性（2026-03-05 更新）：
- `summary.json` 的失败行现在包含 `error`、`error_type`、`error_trace_path`。
- 可直接打开 `error_trace_path` 对应文件定位完整 traceback。

严格失败模式（任一子任务失败即整体非 0）：

```bash
rtos-sim batch-run -b examples/batch_matrix.yaml --strict-fail-on-error
```

---

### 5.5 `compare`（指标对比）

用途：对比两份 metrics JSON，输出差分结果。

```bash
rtos-sim compare \
  --left-metrics artifacts/base_metrics.json \
  --right-metrics artifacts/new_metrics.json \
  --out-json artifacts/compare.json \
  --out-csv artifacts/compare.csv
```

输出重点：
- `scalar_metrics`：双方案标量指标差分
- `core_utilization`：双方案按核利用率差分
- `comparison_mode` / `scenario_labels` / `scenarios`：N-way-ready 报告头与场景明细
- `scalar_summary` / `core_utilization_summary`：为多方案聚合预留的摘要统计；当前 CLI 入口仍是双路输入，但输出结构已可被 UI / 后处理复用

---

### 5.6 `migrate-config`（配置迁移）

用途：迁移旧配置并清理已废弃字段。

```bash
rtos-sim migrate-config \
  --in examples/at01_single_dag_single_core.yaml \
  --out artifacts/migrated.yaml \
  --report-out artifacts/migrate_report.json
```

仅迁移不校验：

```bash
rtos-sim migrate-config --in <old> --out <new> --no-validate
```

---

### 5.7 `plan-static` / `analyze-wcrt` / `export-os-config` / `benchmark-sched-rate`

用途：执行离线规划、WCRT 分析、OS 配置导出与可调度率基准对比。

```bash
rtos-sim plan-static -c examples/at06_time_deterministic.yaml   --out-json artifacts/plan.json

rtos-sim analyze-wcrt -c examples/at06_time_deterministic.yaml   --plan-json artifacts/plan.json   --out-json artifacts/wcrt.json

rtos-sim export-os-config --plan-json artifacts/plan.json   --config examples/at06_time_deterministic.yaml   --out-json artifacts/os.json
```

stochastic arrival CLI 覆盖（不改配置文件）：

```bash
rtos-sim plan-static -c examples/at10_arrival_process.yaml   --task-scope sync_and_dynamic_rt   --arrival-analysis-mode conservative_envelope   --arrival-envelope-min-intervals t_poisson=0.25   --out-json artifacts/plan.at10.json
```

说明：
- `--arrival-analysis-mode sample_path|conservative_envelope`：覆盖 `planning.params.arrival_analysis_mode`。
- `--arrival-envelope-min-intervals task_a=0.25,task_b=1.0`：为 `poisson` / 无法静态求下界的 `custom` arrival 提供保守最小间隔。
- `run --plan-json` 导出的 `runtime_static_windows` 现支持 `release_index`；也可以在原始配置 `scheduler.params.static_windows[*]` 中手工指定，用于绑定特定 release instance。

---

### 5.8 `ui`（可视化界面）

用途：图形化编辑配置、运行仿真、查看 Gantt/日志/对比。

```bash
rtos-sim ui -c examples/at01_single_dag_single_core.yaml
```

若报 `UI dependencies missing`，先安装 UI 依赖：

```bash
python -m pip install -e .[ui]
```

Wayland 兼容说明（2026-03-01 hotfix）：
- 已修复 tooltip popup transient parent 缺失导致的 `qt.qpa.wayland: Failed to create popup` 日志风暴与潜在崩溃。
- 若旧图形栈仍有兼容性问题，可临时使用：

```bash
QT_QPA_PLATFORM=xcb rtos-sim ui -c examples/at01_single_dag_single_core.yaml
```

---

### 5.9 常见返回码速查（自动化脚本必看）

| 命令 | 返回码 | 含义 |
|---|---:|---|
| `validate` | 0 | 配置校验通过 |
| `validate --strict-id-tokens` | 2 | ID 含内部保留分隔符 |
| `run` | 0 | 仿真完成 |
| `run --audit-out` | 2 | 审计失败（规则未通过） |
| `inspect-model --strict-on-fail` | 2 | 关系报告 `status != pass` |
| `batch-run` | 1 | 批配置 `output_dir` 越界或批配置错误 |
| `batch-run --strict-fail-on-error` | 2 | 批跑存在失败子案例 |

---

## 6. UI 操作指南（基础流程）

1. **加载配置**：打开示例 YAML（建议先用 `at01`）。
2. **编辑配置**：
   - 可在结构化表格中编辑任务/资源；
   - 可在 DAG 区域增删节点/边并做自动布局。
3. **校验配置**：点击 Validate（或先走 CLI `validate`）。
4. **运行仿真**：启动后观察 Gantt 与日志；若后续要导出研究报告，务必先完成一次有效运行。
5. **查看详情**：悬停/点击线段查看任务、分段、核心等详情。
6. **Compare 导出**：在右侧 Compare 面板中按顺序装载 scenarios（#1 baseline、#2 focus），可添加 metrics 文件或最近一次运行，支持删除/上移/下移后再点击 `Build Compare`，并导出 JSON / CSV / Markdown；其中 JSON 结构已直接承载 two-way / n-way 摘要字段。
7. **Research Report 导出**：点击工具栏 `Export Research Report`，UI 会复用最近一次运行缓存的 `spec/events` 生成 `research-report.md`、`research-report.json` 与 `research-report-summary.csv`；quality 快照优先使用缓存，缺失时回退读取 `artifacts/quality/quality-snapshot.json`。
8. **审查建议**：保存 YAML，并在最终交付前用 CLI 复跑 events/metrics/audit/model_relations。

提示：UI 不是真相源，但 Compare 与 Research Report 导出已与现有分析产物链保持字段一致，便于研究复核与联调预览。

---

## 7. 测试同学推荐回归清单

### 7.1 功能回归（最小集）

```bash
python -m pytest -q
```

### 7.2 核心链路回归（命令级）

```bash
rtos-sim validate -c examples/at01_single_dag_single_core.yaml --strict-id-tokens
rtos-sim run -c examples/at01_single_dag_single_core.yaml --events-out artifacts/events.jsonl --metrics-out artifacts/metrics.json --audit-out artifacts/audit.json
rtos-sim inspect-model -c examples/at01_single_dag_single_core.yaml --strict-on-fail
rtos-sim batch-run -b examples/batch_matrix.yaml --strict-fail-on-error
```

### 7.3 研究闭环回归（可选）

```bash
python scripts/research_case_suite.py \
  --cases examples/research_counterexamples.json \
  --out-json artifacts/research/research-case-summary.json \
  --out-csv artifacts/research/research-case-summary.csv \
  --audit-dir artifacts/research/case-audits

python scripts/research_report.py \
  --audit artifacts/research/audit.json \
  --relations artifacts/research/model_relations.json \
  --quality artifacts/quality/quality-snapshot.json \
  --out-markdown artifacts/research/research-report.md \
  --out-csv artifacts/research/research-summary.csv \
  --out-json artifacts/research/research-report.json
```

---

## 8. 故障定位与排查矩阵（推荐按顺序）

| 现象 | 快速判定 | 处理建议 |
|---|---|---|
| `validate` 校验 `batch_matrix.yaml` 报错 | 输入是批跑配置，不是运行配置 | 改用 `rtos-sim batch-run -b examples/batch_matrix.yaml` |
| `run --audit-out` 返回码 2 | `audit.status != pass` | 打开 `audit.json`，优先看 `failed_checks` / `issues` |
| `inspect-model --strict-on-fail` 返回码 2 | 关系报告 `status != pass` | 打开 `model_relations.json`，逐条修复 `checks` |
| `batch-run --strict-fail-on-error` 返回码 2 | 至少 1 个子案例失败 | 看 `summary.json` 的 `runs[*].error` 并单例复跑 |
| `batch-run` 提示 `output_dir escapes batch workspace` | 批配置中的 `output_dir` 越界（如 `../`） | 改为批目录内路径，或使用 CLI `--output-dir` 显式指定 |
| UI 启动失败 | 缺 UI 依赖或图形库 | 安装 `.[ui]`，Linux 补系统库 |
| Wayland 下出现 `Failed to create popup` 或 UI 崩溃 | tooltip popup transient parent 不完整（旧版本） | 升级到包含 `fix(ui): avoid Wayland tooltip popup parent crash` 的版本；临时可用 `QT_QPA_PLATFORM=xcb` |

---

## 9. 产物说明（审查常用）

| 文件 | 作用 | 必看字段 |
|---|---|---|
| `events.jsonl` | 事件流，可回放 | `time/type/job_id/segment_id/core_id/payload` |
| `metrics.json` | 指标汇总 | `jobs_completed/deadline_miss_ratio/core_utilization/preempt_count` |
| `audit.json` | 规则审计与画像 | `status/failed_checks/compliance_profiles(research_v1/research_v2)/protocol_proof_assets(rule_version, sample_event_refs, failure_samples)` |
| `model_relations.json` | 模型关系与语义判定 | `status/checks/summary/compliance_profiles` |
| `summary.json`（batch） | 批跑汇总 | `total_runs/succeeded_runs/failed_runs/runs[*].error/error_type/error_trace_path` |
| `research-report.json` | 研究评审报告 | `status/statuses/profiles(research_v1/research_v2)/failed_check_details/non_audit_fail_details` |

---

## 10. 推荐工作流（团队统一）

1. 开发/测试先跑 `validate`。
2. 变更后跑 `run --audit-out`。
3. 变更模型结构时补跑 `inspect-model --strict-on-fail`。
4. 合并前执行 `pytest` 和最小命令级回归。
5. 评审材料统一引用 `artifacts/` 产物。

---

## 11. 命令级决策树（文本版）

```text
开始
 ├─ 先跑 validate
 │   ├─ rc=0 -> 继续 run
 │   ├─ rc=1 -> 修配置语法/语义
 │   └─ rc=2 -> 处理 ID 分隔符风险（strict 模式）
 ├─ 再跑 run --audit-out
 │   ├─ rc=0 -> 继续 inspect-model
 │   ├─ rc=1 -> 看运行错误（配置/运行时）
 │   └─ rc=2 -> 看 audit.failed_checks，修规则失败项
 ├─ 跑 inspect-model --strict-on-fail
 │   ├─ rc=0 -> 可进入 compare / batch-run / 评审
 │   └─ rc=2 -> 看 model_relations.checks，修语义映射
 └─ 跑 batch-run --strict-fail-on-error（如需要）
     ├─ rc=0 -> 批跑通过
     └─ rc=2 -> 从 summary.json 找失败 case，单例复跑
```

---

## 12. 一键回归脚本（本地示例）

```bash
python -m pytest -q && \
rtos-sim validate -c examples/at01_single_dag_single_core.yaml --strict-id-tokens && \
rtos-sim run -c examples/at01_single_dag_single_core.yaml \
  --events-out artifacts/events.jsonl \
  --metrics-out artifacts/metrics.json \
  --audit-out artifacts/audit.json && \
rtos-sim inspect-model -c examples/at01_single_dag_single_core.yaml --strict-on-fail && \
rtos-sim batch-run -b examples/batch_matrix.yaml --strict-fail-on-error
```

---

## 13. 术语速查
- TaskGraph：任务图
- Subtask：DAG 节点
- Segment：最小执行片段
- PIP/PCP：优先级继承/优先级天花板协议
- Hyper-Period：超周期
- compliance_profiles：工程/研究口径的机读验收画像

---

## 14. `legacy/report_api` 兼容层语义边界（I2-2）

`rtos_sim.legacy.report_api` 的定位是“报告同名接口兼容层”，用于保证表 6-2 同名函数可调用；它不是运行时调度器的完整替代。

边界约定：
- `sched_schedule`：只基于 `schedule_table` 在 `now` 时刻做窗口查询，不推进仿真时钟、不维护 ready/running 队列。
- `sched_td_task_new_arrival` / `sched_td_task_complete` / `sched_dy_task_new_arrival` / `sched_dy_task_complete`：只维护轻量 `events/last_event` 历史字典，不触发引擎状态流转。
- `sched_model_change`：只返回配置补丁（如 `static_window_mode/static_windows`），不直接修改运行时引擎对象。

使用建议：
- 需要运行时等价语义（含抢占、资源协议、状态机）时，统一走 `rtos-sim run` 或 `rtos_sim.api` 主链路。
- 兼容层仅用于报告同名接口适配与轻量脚本封装。
