# 测试报告（严格 Docx 收口）

## 1. 基线质量快照

- 来源：`artifacts/quality/quality-snapshot.json`
- 快照时间：2026-03-05T09:44:07Z
- 代码快照：`git_sha=6b240e985bf45bf741df38ea83324257bef92599`
- 结果：`386 passed`，覆盖率 `89.73%`

## 2. 本次改动定向回归

执行命令：

```bash
pytest -q tests/test_cli_planning_commands.py tests/test_sched_rate_benchmark_script.py
pytest -q tests/ui tests/test_ui_gantt.py
```

结果：通过。

## 3. 关键验证结论

1. `plan-static` 产物已包含模型指纹，可用于后续一致性校验。
2. `analyze-wcrt` / `export-os-config` 在 `--strict-plan-match` 下可阻断跨模型误用。
3. `benchmark-sched-rate` 已同时提供严格与兼容口径，并将门禁判定切换到严格口径。
4. UI 已支持 Planning 闭环与四态状态流展示。
5. 随机任务生成功能满足同 seed 可复现。

## 4. 残余风险

1. UI 仍是单文件聚合架构，后续建议继续拆分 `app.py` 职责。

## 5. 本次补充回归（2026-03-05）

执行命令：

```bash
pytest -q tests/test_cli.py tests/test_cli_error_paths.py tests/test_quality_snapshot_script.py tests/test_doc_baseline_consistency.py
python -S scripts/quality_snapshot.py --help
python -m pytest -q
```

结果：通过。

关键结论：

1. `batch-run` 已对批配置中的 `output_dir` 启用边界约束，`../` 越界路径会被拒绝并返回错误。
2. `batch-run` 失败子案例的 `summary.json` 记录已包含 `error_type` 与 `error_trace_path`，可直接定位 traceback。
3. `quality_snapshot.py` 在 `--reuse-existing-artifacts` 场景下，若摘要解析为失败且未启用 `--allow-fail`，会返回非 0 退出码，避免 CI 假阳性。
4. `quality_snapshot.py` 已通过“最小环境入口”回归：`python -S scripts/quality_snapshot.py --help` 可直接运行，避免 `ModuleNotFoundError: rtos_sim`。

## 6. 本轮收口补充回归（2026-03-06）

执行命令：

```bash
python scripts/check_doc_baseline_consistency.py --snapshot artifacts/quality/quality-snapshot.json --docs-root docs
python scripts/check_doc_reference_integrity.py --repo-root .
python -m rtos_sim.cli.main batch-run -b examples/batch_matrix.yaml
python -m pytest -q
ruff check .
```

结果：通过。

关键结论：

1. `quality_snapshot` 已支持 quiet-progress 回退解析，`pytest --cov ... -q` 场景不再误判失败。
2. 文档基线一致性与引用完整性已形成双校验，路径存在与行号越界问题可在 CI 阻断。
3. `batch-run` 示例配置已可开箱运行，README 命令链路与样例配置一致。
4. 规划器新增 `np_rm` 与 `precautious_rm`，并完成 CLI/API/UI 接入与测试覆盖。
5. 调度率基准脚本已输出 `standard_seeded` 与 `docx_mixed` 双 profile，门禁按双口径并列执行。
