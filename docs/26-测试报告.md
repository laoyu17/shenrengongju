# 测试报告（严格 Docx 收口）

## 1. 基线质量快照

- 来源：`artifacts/quality/quality-snapshot.json`
- 快照时间：2026-03-05T04:39:21Z
- 结果：`357 passed`，覆盖率 `89.68%`

## 2. 本次改动定向回归

执行命令：

```bash
pytest -q tests/test_cli_planning_commands.py tests/test_sched_rate_benchmark_script.py
pytest -q tests/ui tests/test_ui_gantt.py
```

结果：通过。

## 3. 关键验证结论

1. `plan-static` 产物已包含模型指纹，可用于后续一致性校验。
2. `analyze-wcrt` / `export-os-config` 在 `--strict-plan-match` 下可阻断跨模型误用。
3. `benchmark-sched-rate` 已同时提供严格与兼容口径，并将门禁判定切换到严格口径。
4. UI 已支持 Planning 闭环与四态状态流展示。
5. 随机任务生成功能满足同 seed 可复现。

## 4. 残余风险

1. UI 仍是单文件聚合架构，后续建议继续拆分 `app.py` 职责。
