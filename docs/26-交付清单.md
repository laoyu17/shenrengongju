# 交付清单（严格 Docx 收口）

## 1. 代码交付

1. `rtos_sim/api.py`：指纹提取 + 双口径可调度率
2. `rtos_sim/cli/main.py`：`--strict-plan-match` + 输出口径扩展
3. `scripts/benchmark_sched_rate.py`：严格口径门禁
4. `rtos_sim/ui/controllers/planning_controller.py`：UI 规划控制器
5. `rtos_sim/ui/controllers/timeline_controller.py`：四态状态流
6. `rtos_sim/ui/controllers/telemetry_controller.py`：Telemetry 交互与状态面板控制
7. `rtos_sim/ui/controllers/gantt_style_controller.py`：Gantt 样式与图例缓存控制
8. `rtos_sim/ui/panel_builders.py`：Planning/Compare 面板装配下沉
9. `rtos_sim/ui/panel_state.py`：UI 面板状态容器
10. `rtos_sim/ui/app.py`：入口组装与控制器委托
11. `rtos_sim/cli/parser_builder.py`：CLI 参数定义与命令绑定构建
12. `rtos_sim/cli/handlers_planning.py`：规划相关 CLI 处理器拆分
13. `rtos_sim/ui/config_doc.py`：planning 字段读写支持
14. `review/scripts/i2_freeze_delivery_baseline.sh`：交付基线快照冻结脚本
15. `review/scripts/strict_plan_pipeline.sh`：strict 默认脚手架模板
16. `rtos_sim/analysis/compare.py`：Compare 报告 N-way-ready 聚合结构与 Markdown 渲染
17. `rtos_sim/ui/compare_io.py`：Compare JSON/CSV/Markdown 导出
18. `rtos_sim/ui/controllers/compare_controller.py`：Compare Markdown 导出入口
19. `rtos_sim/ui/controllers/run_controller.py`：UI 最近一次运行缓存（payload/spec/events）
20. `rtos_sim/ui/controllers/research_report_controller.py`：UI 研究报告导出入口

## 2. 测试交付

1. `tests/test_cli_planning_commands.py`
2. `tests/test_sched_rate_benchmark_script.py`
3. `tests/ui/test_timeline_controller.py`
4. `tests/test_ui_gantt.py`
5. `tests/test_ci_strict_plan_match_gate.py`
6. `tests/test_compare.py`
7. `tests/test_ui_compare_io.py`
8. `tests/ui/test_compare_controller.py`
9. `tests/ui/test_run_controller.py`
10. `tests/test_ui_worker.py`
11. `tests/ui/test_research_report_controller.py`

## 3. 文档交付

1. `docs/26-严格Docx验收矩阵.md`
2. `docs/26-需求分析报告.md`
3. `docs/26-测试细则.md`
4. `docs/26-测试报告.md`
5. `docs/26-用户维护及使用手册.md`
6. `docs/26-研制总结报告.md`
7. `docs/26-用户培训记录.md`
8. `docs/26-交付清单.md`
9. `docs/10-详细设计说明书.md`
10. `docs/11-实施现状问题与Sprint规划.md`
11. `docs/19-用户使用说明书.md`
12. `docs/22-分阶段验收报告.md`
13. `review/02-审查总报告.md`
14. `review/06-收口执行记录.md`

## 4. 验收命令清单

```bash
pytest -q tests/test_cli_planning_commands.py tests/test_sched_rate_benchmark_script.py
pytest -q tests/ui tests/test_ui_gantt.py
pytest -q tests/test_compare.py tests/test_ui_compare_io.py tests/ui/test_compare_controller.py
pytest -q tests/ui/test_run_controller.py tests/test_ui_worker.py tests/ui/test_research_report_controller.py tests/test_ui_gantt.py
python scripts/check_doc_baseline_consistency.py --snapshot artifacts/quality/quality-snapshot.json --docs-root docs
bash review/scripts/strict_plan_pipeline.sh examples/at06_time_deterministic.yaml
bash review/scripts/i2_freeze_delivery_baseline.sh
```

## 5. 本轮完成项

- 代码：已交付
- 测试：已交付
- 文档：已交付
- 本轮完成项：质量快照、严格口径门禁、Phase 2/3 研究化链路，以及 Phase 4 的 Compare / Research Report 最小闭环已收口
- 未完成边界：Compare UI 仍保留 left/right 双路装载与构建，DAG 多选 / 批量移动 / 批量删除未在本轮实现
- 后续建议：最终 freeze 需在干净工作区重新生成 `snapshot_meta.json`

## 6. I1-4 基线冻结产物目录

冻结脚本输出目录：`review/runtime/i2/baseline_freeze`
本次冻结快照：`review/runtime/i2/baseline_freeze_20260305`

说明：上述目录保留的是 2026-03-05 的历史冻结记录；其中 `review/runtime/i2/baseline_freeze/snapshot_meta.json` 固定的 `head_commit=be6d01360f544d9a239f32ff0ae4b434d6bdf4a7`，且 `dirty_workspace=true`。当前工作区基线已推进到 `workspace_git_sha=2f6e8911c65170af321078bc8e22e7964f67812c`，因此这批 freeze 产物仅作为历史证据保留，最终 release/freeze 仍需在干净工作区重跑。

目录结构：
- `change_list/`：`git_status_short.txt`、`git_diff_stat.txt`、`git_diff_name_status.txt`
- `artifacts/`：审查关键产物快照副本（runtime/CI gate/口径门禁/台账/脚本）
- `snapshot_meta.json`：快照元数据（`head_commit`、`tag_name`、`dirty_workspace`）
- `manifest.tsv`：冻结目录文件清单（sha256 + size）
- `tag.txt`：本次基线标签信息

## 7. 后续建议

- 在干净工作区复跑 `review/scripts/i2_freeze_delivery_baseline.sh`，确保 `snapshot_meta.json` 中 `head_commit == HEAD` 且 `dirty_workspace=false`。
- 当前交付状态可表述为“可复核、未最终冻结”：质量快照与门禁已收口，但当前 HEAD 尚缺 clean-workspace freeze。
- Phase 4 后续重点转向 DAG 多选 / 批量移动 / 批量删除，以及 Compare 的 N-way 场景装载 UI，而不是重复建设第二套报告引擎。
