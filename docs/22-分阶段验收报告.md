# 分阶段验收报告（S1~S4）

## 0. 文档控制
- 版本：v1.1
- 日期：2026-03-04
- 审查范围：`project/`（docs / scripts / rtos_sim / tests）
- 代码快照：`git_sha=d8fd356efbd34b71423c63185a457ce77e4e147d`
- 质量快照：`artifacts/quality/quality-snapshot.json`
  - `pytest=361 passed`
  - `coverage=89.74%`（`line_rate=89.74056603773585`）

## 1. 阶段执行结果

### S1 基线收口（完成）
- 已统一 `docs/10`、`docs/11`、`docs/14`、`docs/18`、`docs/21` 的 `git_sha` 与质量快照事实源。
- 已在台账追加收口记录：`docs/20-审查问题台账.csv`（REV-015）。

### S2 审查证据自动化（完成）
- 新增脚本：`scripts/check_doc_baseline_consistency.py`
- 新增测试：`tests/test_doc_baseline_consistency.py`
- README 已补充一键校验命令入口：`README.md`

### S3 可维护性拆分（完成）
- 已将 FR-13 Compare 面板的 I/O 逻辑从 `rtos_sim/ui/app.py` 拆分至 `rtos_sim/ui/compare_io.py`。
- 新增回归：`tests/test_ui_compare_io.py`，并与 `tests/test_ui_gantt.py` 联合通过。

### S4 回归与文档闭环（完成）
- 已更新综合审查结论与证据引用：`docs/18-综合审查报告-2026-02-24.md`
- 已更新审查台账收口记录：`docs/20-审查问题台账.csv`
- 已更新基线一致性校验记录：`docs/21-全量基线一致性校验记录-2026-02-24.md`

### S4-HF1 Wayland UI 热修复（完成）
- 修复 Wayland 下 tooltip popup transient parent 缺失导致的日志风暴与潜在崩溃：`rtos_sim/ui/app.py:1919`
- 新增回归：`tests/test_ui_gantt.py:113`
- 文档同步：`docs/11-实施现状问题与Sprint规划.md`、`docs/18-综合审查报告-2026-02-24.md`、`docs/19-用户使用说明书.md`、`docs/20-审查问题台账.csv`

### S5-PAPER 图件流水线与混合模式（完成）
- 约束确认：不扩展产品功能，仅建设投稿图件离线生产链与文档规范。
- 数据与图件流水线已落地：`scripts/paper_assets/build_dataset.py`、`scripts/paper_assets/plot_main_figures.py`、`scripts/paper_assets/plot_appendix_figures.py`、`scripts/paper_assets/export_manifest.py`
- 已生成主文图/附录图（PDF + PNG）与 manifest：`artifacts/paper_figures/main/`、`artifacts/paper_figures/appendix/`、`artifacts/paper_figures/manifest.json`
- 已沉淀外部概念图 Prompt 包（Nano Banana Pro）：`scripts/paper_assets/prompt_nano_banana.md`
- 混合模式口径：核心科研图本地直出；概念图外部模型绘制并受 Prompt 约束。

### S6-治理深化（Phase B 第二刀 + 第三刀 + CLI 二次拆分，完成）
- UI 结构继续下沉：新增 `panel_state`/`panel_builders`/`telemetry_controller`/`gantt_style_controller`，`app.py` 仅保留组装与委托。
  - 关键文件：`rtos_sim/ui/panel_state.py`、`rtos_sim/ui/panel_builders.py`、`rtos_sim/ui/controllers/telemetry_controller.py`、`rtos_sim/ui/controllers/gantt_style_controller.py`
- CLI 参数定义与处理器解耦：参数统一由 `parser_builder` 构建，规划三命令处理器下沉至 `handlers_planning`。
  - 关键文件：`rtos_sim/cli/parser_builder.py`、`rtos_sim/cli/handlers_planning.py`
- 规模指标：`rtos_sim/cli/main.py` 已收敛至 `<700`（当前约 `532` 行），满足本轮治理目标。
- CI 严格口径防回退门禁已补：新增 `tests/test_ci_strict_plan_match_gate.py`，防止脚本遗漏 `--strict-plan-match`。

## 2. 回归与门禁记录

已执行并通过：

```bash
python -m pytest -q tests/test_ui_compare_io.py tests/test_ui_gantt.py tests/test_audit.py
python -m pytest -q
python scripts/quality_snapshot.py --output artifacts/quality/quality-snapshot.json --coverage-json artifacts/quality/coverage.json
python scripts/check_doc_baseline_consistency.py --snapshot artifacts/quality/quality-snapshot.json --docs-root docs
python -m rtos_sim.cli.main validate -c examples/at01_single_dag_single_core.yaml --strict-id-tokens
python -m rtos_sim.cli.main run -c examples/at01_single_dag_single_core.yaml --events-out artifacts/plan/s4-events.jsonl --metrics-out artifacts/plan/s4-metrics.json --audit-out artifacts/plan/s4-audit.json
for f in examples/at0{1..9}_*.yaml examples/at10_*.yaml; do python -m rtos_sim.cli.main inspect-model -c "$f" --strict-on-fail; done
python -m rtos_sim.cli.main batch-run -b examples/batch_matrix.yaml --strict-fail-on-error
python scripts/research_case_suite.py --cases examples/research_counterexamples.json --out-json artifacts/plan/s4-research-case-summary.json --out-csv artifacts/plan/s4-research-case-summary.csv --audit-dir artifacts/plan/s4-case-audits
```

## 3. 当前剩余风险（open）

- REV-004：`rtos_sim/ui/app.py` 仍为高聚合文件，需继续分层拆分。
- REV-008：UI 复杂交互边界仍需持续补测与阈值治理。
- REV-014：`engine/audit/ui` 超长函数与高聚合结构仍有维护成本风险。

> 本轮未发现阻断主链路（`validate/run/inspect-model/batch-run`）的 P0 缺陷。
