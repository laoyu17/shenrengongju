# 根目录 Docx 基线全量审查总报告（2026-03-04）

## 1. 审查结论

在“仅以两份根目录 Docx 为需求基线”的前提下，当前工作区在功能实现与测试覆盖层面已形成闭环，主链路可复跑，负路径可阻断。

- 功能主链路复核：通过
- 负路径复核：通过（返回码符合预期）
- Docx 8 条目标主评分：**93.75 / 100**
- 距离“满足要求”的剩余差距：**6.25 分**（集中在“30%可调度率目标口径冻结与交付一致性”）

说明：本报告不把性能优化作为目标，仅评估功能正确性、设计一致性、可维护性与交付风险。

## 2. 审查范围与事实源

- 代码范围：`/mnt/hgfs/_code/simpytool/project` 当前工作区（含未提交改动）
- 需求基线：
  - `250909-仿真工具-基础模型.docx`
  - `异构计算平台混合实时任务调度规划与仿真工具设计方案报告.docx`
- Docx 提取与锚点：`review/docx/baseline_*_extracted.md`
- 运行事实：`review/runtime/runtime_evidence.json`
- 质量事实源：`artifacts/quality/quality-snapshot.json`（`git_sha=6b240e985bf45bf741df38ea83324257bef92599`，`pytest=386 passed`，`coverage=89.73%`）

## 3. 运行核验结果

### 3.1 全量回归

- `python -m pytest -q`：`386 passed`（全绿）
- `pytest --collect-only -q`：仅用于收集规模核对，不作为通过数口径

### 3.2 主链路命令

- `validate`：`rc=0`（`review/runtime/logs/01_validate_at01.log`）
- `run`：`rc=0`（`review/runtime/logs/02_run_at01.log`、`15_run_at05_preempt.log`）
- `inspect-model --strict-on-fail`：`rc=0`（`review/runtime/logs/03_inspect_at01_strict.log`）
- `plan-static`：`rc=0`，输出 `spec_fingerprint`
- `analyze-wcrt --strict-plan-match`：`rc=0`
- `export-os-config --strict-plan-match`：`rc=0`
- `benchmark-sched-rate`：`rc=0`
- `migrate-config`：`rc=0`

### 3.3 负路径

- 计划/模型指纹不匹配：`analyze-wcrt --strict-plan-match` 返回 `rc=2`
- 严格导出缺 `--config`：`export-os-config --strict-plan-match` 返回 `rc=2`
- 无效窗口约束（同核重叠）：`validate` 返回 `rc=1`
- 扩展验证：`inspect-model --strict-on-fail` 对 `at01~at10` 全部 `rc=0`

## 4. Docx 8 条目标评分

评分规则：每项 12.5 分；`已实现=1`、`部分实现=0.5`、`未实现=0`。

| 条款 | 判定 | 得分 |
|---|---|---|
| G-01 固定点任务规划 | 已实现 | 12.5 |
| G-02 固定点+定期+间歇混合调度 | 已实现 | 12.5 |
| G-03 混合任务可调度性分析 | 已实现 | 12.5 |
| G-04 可调度率提升30% | 部分实现 | 6.25 |
| G-05 图形化输入输出 | 已实现 | 12.5 |
| G-06 参数配置能力 | 已实现 | 12.5 |
| G-07 运行时动态仿真与状态展示 | 已实现 | 12.5 |
| G-08 OS参数导出接口 | 已实现 | 12.5 |

总分：**93.75 / 100**。

判定依据详见：`review/01-Docx条款追踪矩阵.md`。

## 5. 缺陷与风险分级（P0-P3）

- P0：未发现阻断主链路的问题。
- P1：
  - `RISK-001`：30%目标口径存在双轨结果（小样本0提升 vs 套件83.3%），需冻结唯一验收口径。
  - `RISK-002`：工作区存在 54 条未提交变更，交付基线风险高。
- P2：
  - `RISK-003`：`--strict-plan-match` 非默认，误用风险仍依赖调用方习惯。
  - `RISK-004`：`static_windows` 约束主要在 build 期，不在 schema 字段层强约束。
- P3：
  - `RISK-005`：`legacy/report_api` 的轻量别名语义需持续防漂移。

详单见：`review/03-问题台账.csv`。

## 6. 距离满足要求还有多远

按当前证据，功能闭环已基本达成，差距主要集中在“验收口径治理 + 交付基线治理”。

- 功能实现差距：小（主链路和负路径均可复跑）
- 设计一致性差距：中（30%目标需统一口径并固定数据集）
- 工程治理差距：中（需冻结基线并纳入CI门禁）

建议按两迭代收口：`review/04-两迭代收口路线图.md`。
