# 根目录 Docx 基线全量审查总报告（2026-03-07）

## 1. 审查结论

在“仅以两份根目录 Docx 为需求基线”的前提下，当前工作区在功能实现、测试覆盖、30% 可调度率验收口径，以及 Phase 4 最小研究工作台能力上已形成闭环，主链路可复跑，负路径可阻断。

- 功能主链路复核：通过
- 负路径复核：通过（返回码符合预期）
- Docx 8 条目标主评分：**100 / 100**
- 距离“功能满足”的剩余差距：**0 分**（按当前 Docx 功能目标口径）
- 距离“可冻结交付”的剩余差距：**1 项**（当前 `workspace_git_sha` 的 clean-workspace freeze）

说明：本报告不把性能优化作为目标；当前剩余事项主要属于交付基线冻结治理与 UI 高级交互 backlog，而非核心需求缺口。

## 2. 审查范围与事实源

- 代码范围：`/mnt/hgfs/_code/simpytool/project` 当前工作区（含未提交改动）
- 需求基线：
  - `250909-仿真工具-基础模型.docx`
  - `异构计算平台混合实时任务调度规划与仿真工具设计方案报告.docx`
- Docx 提取与锚点：`review/docx/baseline_*_extracted.md`
- 运行事实：`review/runtime/runtime_evidence.json`
- 质量事实源：`artifacts/quality/quality-snapshot.json`（`evidence_git_sha=2f6e8911c65170af321078bc8e22e7964f67812c`，`pytest=424 passed`，`coverage=89.29%`）
- 工作区基线：`workspace_git_sha=2f6e8911c65170af321078bc8e22e7964f67812c`
- 正式 clean freeze 产物：`review/runtime/i2/clean_freeze/snapshot_meta.json`（`head_commit=4ae4daa823c9377474dcb49c68006d46105ae7a9`，`dirty_workspace=false`，`freeze_kind=clean_formal`，`tag_name=delivery-baseline-20260307-103952`）；历史 `review/runtime/i2/baseline_freeze/*` 仅继续作为 dirty 证据归档
- Phase 4 当前状态补充：Compare 报告与导出层已 N-way-ready，UI 研究报告导出入口已接入；DAG 多选/批量编辑仍未纳入本轮交付。

## 3. 运行核验结果

### 3.1 全量回归

- `python -m pytest -q`：`424 passed`（全绿）
- `pytest --collect-only -q`：仅用于收集规模核对，不作为通过数口径

### 3.2 主链路命令

- `validate`：`rc=0`（`review/runtime/logs/01_validate_at01.log`）
- `run`：`rc=0`（`review/runtime/logs/02_run_at01.log`、`15_run_at05_preempt.log`）
- `inspect-model --strict-on-fail`：`rc=0`（`review/runtime/logs/03_inspect_at01_strict.log`）
- `plan-static`：`rc=0`，输出 `spec_fingerprint`
- `analyze-wcrt --plan-json`（默认 strict）：`rc=0`
- `export-os-config --config ... --plan-json`（默认 strict）：`rc=0`
- `benchmark-sched-rate`：`rc=0`
- `migrate-config`：`rc=0`

### 3.3 负路径

- 计划/模型指纹不匹配：`analyze-wcrt --plan-json` 默认返回 `rc=2`
- 默认 strict 导出缺 `--config`：`export-os-config --plan-json` 返回 `rc=2`
- 无效窗口约束（同核重叠）：`validate` 返回 `rc=1`
- 扩展验证：`inspect-model --strict-on-fail` 对 `at01~at10` 全部 `rc=0`

## 4. Docx 8 条目标评分

评分规则：每项 12.5 分；`已实现=1`、`部分实现=0.5`、`未实现=0`。

| 条款 | 判定 | 得分 |
|---|---|---|
| G-01 固定点任务规划 | 已实现 | 12.5 |
| G-02 固定点+定期+间歇混合调度 | 已实现 | 12.5 |
| G-03 混合任务可调度性分析 | 已实现 | 12.5 |
| G-04 可调度率提升30% | 已实现 | 12.5 |
| G-05 图形化输入输出 | 已实现 | 12.5 |
| G-06 参数配置能力 | 已实现 | 12.5 |
| G-07 运行时动态仿真与状态展示 | 已实现 | 12.5 |
| G-08 OS参数导出接口 | 已实现 | 12.5 |

总分：**100 / 100**。

判定依据详见：`review/01-Docx条款追踪矩阵.md`。

## 5. 缺陷与风险分级（P0-P3）

- P0：未发现阻断主链路的问题。
- P1：
  - `RISK-002`：已关闭。正式 clean freeze 已在 `head_commit=4ae4daa823c9377474dcb49c68006d46105ae7a9` 上完成，当前交付基线已从历史 dirty 证据切换到正式冻结点。
- P2：
  - `RISK-003`：默认 strict 已落地，误用风险已从“调用方习惯”收口到显式 `--allow-plan-mismatch`。
  - 审查观察：Phase 4 的 Compare UI 已切到 ordered scenarios 列表，N-way 装载/排序/构建/导出闭环已完成。
  - 审查观察：DAG 多选 / 批量移动 / 批量删除尚未交付，复杂图编辑仍有体验缺口。
- P3：
  - 无新增 open 的 P3 风险；`RISK-001`、`RISK-004`、`RISK-005` 已转为已关闭或历史风险。

详单见：`review/03-问题台账.csv`。

## 6. 距离满足要求还有多远

按当前证据，Docx 功能目标已满足；当前差距主要集中在“干净工作区 freeze 治理 + Phase 4 高级交互 backlog”，而不是核心能力缺失。

- 功能实现差距：无（主链路和负路径均可复跑，30% 门禁已按 `candidate_only_uplift` 通过）
- 设计一致性差距：小（Compare / Research Report 已与现有分析产物链对齐，剩余问题集中在 UI 交互深度）
- 工程治理差距：中（仍需在干净工作区重做最终 freeze，并收敛主窗口高聚合热点）

建议继续按收口计划推进：先完成最终 freeze，再处理 DAG 多选/批量编辑与 Compare 的 N-way 场景装载 UI。


## 7. Phase 4 当前状态补充

- Compare：CLI / UI 共用的 compare report 已新增 `comparison_mode`、`scenario_labels`、`scenarios`、`scalar_summary`、`core_utilization_summary`，并已支持 Markdown 导出。
- Research Report：UI 工具栏已新增 `Export Research Report`，直接复用 `build_model_relations_report`、`build_audit_report`、`build_research_report_payload` 与 `render_research_report_markdown`。
- 当前边界：Compare 的 ordered scenarios UI 已完成；DAG 多选 / 批量移动 / 批量删除尚未纳入本轮实现。
- 审查判断：Phase 4 已形成“研究报告工作台最小闭环”，但不应把其表述为“UI 全量重构完成”。
