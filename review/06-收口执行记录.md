# 收口执行记录（I2 + I1-CI 衔接 + Phase 4 追加）

日期：2026-03-08
工作区：`/mnt/hgfs/_code/simpytool/project`
范围：按 `review/04-两迭代收口路线图.md` 执行 I2-1、I2-2，并完成 I1 脚本 CI 衔接；当前追加 Phase 4 Compare / Research Report 最小闭环记录。
约束：未进行性能优化；本轮完成 UI 壳化深拆，但保持 CLI/YAML/导出公开行为兼容。
质量事实源：`artifacts/quality/quality-snapshot.json`（`evidence_git_sha=de76ff299bea0557032a40c7117b78e16866d1a1`，`pytest=482 passed`，`coverage=89.98%`）
工作区基线：`workspace_git_sha=de76ff299bea0557032a40c7117b78e16866d1a1`

## 1. 本轮执行项与状态

| 项目 | 状态 | 说明 | 证据 |
|---|---|---|---|
| A. I2-1 配置治理（`static_window_mode/static_windows`） | 已关闭 | 在 Schema 层新增字段与窗口对象约束，错误可定位到 `scheduler.params.static_windows.*` | `rtos_sim/io/schema.py`、`tests/test_engine_static_window_mode.py` |
| B. I2-2 接口治理（`legacy/report_api`） | 已关闭 | 明确兼容层语义边界（代码注释+用户文档），并补充契约测试防漂移 | `rtos_sim/legacy/report_api.py`、`docs/19-用户使用说明书.md`、`tests/test_cli_planning_commands.py` |
| C. I1 与 CI 衔接 | 已关闭 | 新增 CI job 执行 `review/scripts/i1_ci_gate.sh`，并在失败时提供 `results.tsv/logs` 诊断与工件上传 | `.github/workflows/ci.yml` |
| D. 审查工件更新 | 已关闭 | 更新问题台账状态并新增本执行记录 | `review/03-问题台账.csv`、`review/06-收口执行记录.md` |

## 2. 验收命令结果（本次实跑）

1. `python -m pytest -q`
   - 结果：通过（`482 passed`）
   - 返回码：`0`

2. `bash review/scripts/i1_freeze_sched_rate_gate.sh`
   - 结果：通过，输出 `review/runtime/i1/sched_rate_gate/benchmark_frozen.json`、`benchmark_frozen.csv`、`summary.txt`
   - 返回码：`0`

3. `bash review/scripts/i1_ci_gate.sh`
   - 结果：通过，输出 `review/runtime/i1/ci_gate/results.tsv` 与 `review/runtime/i1/ci_gate/logs/*.log`
   - 返回码：`0`

4. 定向复跑（文件级）
   - `python -m pytest -q tests/test_engine_static_window_mode.py tests/test_cli_planning_commands.py`
   - 返回码：`0`

## 3. 风险台账同步结果

| 风险ID | 原状态 | 新状态 | 结论 |
|---|---|---|---|
| RISK-001 | open | closed | 口径冻结与 CI 统一入口已落地并复跑通过 |
| RISK-002 | open | partial | 已具备快照冻结能力，但工作区仍未完成独立冻结分支收口 |
| RISK-003 | open | partial | CI 模板已强制 strict 链路，但 CLI 默认语义仍为“非 strict 不阻断” |
| RISK-004 | open | closed | Schema 字段约束与字段级报错定位已落地 |
| RISK-005 | open | closed | 兼容层语义边界文档化并有契约测试 |

## 4. 备注

- 本次未改动调度算法与性能参数。
- 既有未提交改动保持原样，未执行破坏性回退命令。

## 5. 固定更新机制（后续每轮必做）

每轮迭代完成后，必须在本文件追加一个“增量执行记录”小节，至少包含：

1. 本轮目标与对应风险ID（如 `RISK-002/RISK-003`）。
2. 实际变更文件与关键证据路径。
3. 验收命令、返回码与关键输出目录。
4. 风险状态变更（`open/partial/closed`）及理由。

固定命名建议：
- 小节标题：`## <日期>（迭代N）增量执行记录`
- 状态标签：`已关闭 / 部分关闭 / 未关闭`

## 6. 2026-03-05（迭代追加）增量执行记录

### 6.1 目标

- RISK-002：完成一次可复跑快照冻结（变更清单 + 标签 + 产物目录）。
- RISK-003：继续下沉 strict 模板到默认脚手架/文档命令模板。

### 6.2 实施与证据

- 基线冻结脚本：`review/scripts/i2_freeze_delivery_baseline.sh`
  - 输出：`review/runtime/i2/baseline_freeze/change_list/*`
  - 标签：`review/runtime/i2/baseline_freeze/tag.txt`
  - 产物目录：`review/runtime/i2/baseline_freeze/artifacts/*`
- strict 默认脚手架：`review/scripts/strict_plan_pipeline.sh`
  - 输出：`review/runtime/i2/strict_plan_pipeline/results.tsv`
  - 诊断：`review/runtime/i2/strict_plan_pipeline/logs/*.log`
- 文档模板更新：
  - `docs/26-用户维护及使用手册.md`（新增 strict 默认脚手架章节）
  - `docs/26-交付清单.md`（新增 I1-4 冻结产物目录说明）

### 6.3 风险状态更新建议

| 风险ID | 上轮状态 | 本轮状态 | 说明 |
|---|---|---|---|
| RISK-002 | open | partial | 已产出快照冻结与标签，但工作区仍为 dirty，未形成独立冻结分支 |
| RISK-003 | partial | partial | strict 已下沉到脚本+文档模板，CLI 默认语义仍保持兼容不变 |

## 7. 2026-03-05（迭代追加-2）增量执行记录

### 7.1 目标

- 仅保留一个最终冻结标签（清理重跑标签）。
- 将 `strict_plan_pipeline.sh` 接入 CI 作为独立非性能门禁。

### 7.2 实施与证据

- 标签清理：
  - 删除：`delivery-baseline-20260305-034400`
  - 保留：`delivery-baseline-20260305-034444`
  - 证据：`git tag --list 'delivery-baseline-*'`
- CI 门禁接入：
  - 新增 job：`strict_plan_gate`
  - 路径：`.github/workflows/ci.yml`
  - 运行脚本：`review/scripts/strict_plan_pipeline.sh`
  - 产物目录：`review/runtime/i2/strict_plan_pipeline_ci`
  - 诊断入口：`results.tsv` + `logs/*.log`

### 7.3 风险状态更新建议

| 风险ID | 上轮状态 | 本轮状态 | 说明 |
|---|---|---|---|
| RISK-002 | partial | partial | 标签清理完成，但工作区仍 dirty，未形成独立冻结分支 |
| RISK-003 | partial | partial | CI 已新增 strict 独立门禁，但 CLI 默认语义未改 |

## 8. 2026-03-06（治理收口 + RM 扩展）增量执行记录

### 8.1 目标

- 先恢复验收门禁可信度：修复质量快照解析脆弱点、补齐 lint 硬门禁、修复批跑示例开箱失败。
- 再完成口径与证据链收口：文档基线一致、Docx 引用锚点完整、空问题集可解释。
- 实现 RM 算法扩展并保持默认候选兼容：新增 `np_rm/precautious_rm`，仅并行观测不改默认硬门禁候选。

### 8.2 实施与证据

- P0 门禁可信度收口：
  - `rtos_sim/analysis/quality_snapshot.py`
  - `.github/workflows/ci.yml`
  - `examples/batch_matrix.yaml`
  - `README.md`
- P1 基线与引用校验：
  - `artifacts/quality/quality-snapshot.json`
  - `scripts/check_doc_reference_integrity.py`
  - `tests/test_doc_reference_integrity.py`
  - `docs/25-设计方案报告全量追踪矩阵.md`
  - `review/00-Docx条款主索引.md`
  - `review/01-Docx条款追踪矩阵.md`
- P2 算法扩展与口径增强：
  - `rtos_sim/planning/heuristics.py`
  - `rtos_sim/planning/__init__.py`
  - `rtos_sim/cli/parser_builder.py`
  - `rtos_sim/ui/app.py`
  - `rtos_sim/api.py`
  - `scripts/benchmark_sched_rate.py`
- P3 双口径硬门禁固化：
  - `.github/workflows/ci.yml`

### 8.3 验收命令与结果

1. `python scripts/check_doc_baseline_consistency.py --snapshot artifacts/quality/quality-snapshot.json --docs-root docs`
   - 返回码：`0`
2. `python scripts/check_doc_reference_integrity.py --repo-root .`
   - 返回码：`0`
3. `python -m rtos_sim.cli.main batch-run -b examples/batch_matrix.yaml`
   - 返回码：`0`
4. `python -m pytest -q`
   - 返回码：`0`（`482 passed`）
5. `ruff check .`
   - 返回码：`0`

### 8.4 风险状态更新建议

| 风险ID | 上轮状态 | 本轮状态 | 说明 |
|---|---|---|---|
| RISK-002 | partial | partial | 已完成双口径并列门禁与基线更新，冻结分支治理仍待单独流程闭环 |
| RISK-003 | partial | partial | CI 已强制 strict，CLI 默认兼容语义保持不变 |
| RISK-006 | open | closed | 文档引用锚点已可校验并阻断，路径与行号失效问题已纳入门禁 |
| RISK-007 | open | closed | 质量快照摘要解析已双侧加固，`-q` 场景不再导致假失败 |


## 9. 2026-03-07（Phase 4 当前进度）增量执行记录

### 9.1 目标

- 完成 Phase 4 的最小研究工作台闭环：先做 Compare 报告结构与导出层 N-way-ready，再接入 UI 研究报告导出入口。
- 保持边界：不重写 UI，不启动 DAG 多选 / 批量移动 / 批量删除的大规模重构。
- 关联项：`RISK-002`（交付事实持续以证据闭环为准）、`REV-008` / `REV-014`（UI 热点与复杂交互仍需后续治理）。

### 9.2 实施与证据

- Compare/report 最小收口：
  - `rtos_sim/analysis/compare.py`
  - `rtos_sim/analysis/__init__.py`
  - `rtos_sim/ui/compare_io.py`
  - `rtos_sim/ui/controllers/compare_controller.py`
  - `rtos_sim/ui/app.py`
  - `rtos_sim/ui/panel_builders.py`
- Research Report UI 导出最小接入：
  - `rtos_sim/ui/controllers/run_controller.py`
  - `rtos_sim/ui/worker.py`
  - `rtos_sim/ui/controllers/research_report_controller.py`
  - `rtos_sim/ui/controllers/__init__.py`
  - `rtos_sim/ui/app.py`
- 回归与文档同步：
  - `tests/test_compare.py`
  - `tests/test_ui_compare_io.py`
  - `tests/ui/test_compare_controller.py`
  - `tests/ui/test_run_controller.py`
  - `tests/ui/test_research_report_controller.py`
  - `docs/10-详细设计说明书.md`
  - `docs/11-实施现状问题与Sprint规划.md`
  - `docs/19-用户使用说明书.md`
  - `docs/22-分阶段验收报告.md`
  - `docs/26-用户维护及使用手册.md`
  - `docs/26-用户培训记录.md`
  - `review/06-收口执行记录.md`

### 9.3 验收命令与结果

1. `python -m pytest -q tests/test_compare.py tests/test_ui_compare_io.py tests/ui/test_compare_controller.py`
   - 返回码：`0`
   - 摘要：`12 passed`
2. `python -m pytest -q tests/ui/test_run_controller.py tests/test_ui_worker.py tests/ui/test_research_report_controller.py tests/test_ui_gantt.py`
   - 返回码：`0`
   - 摘要：全部通过（`[100%]`）
3. `python scripts/check_doc_baseline_consistency.py --snapshot artifacts/quality/quality-snapshot.json --docs-root docs`
   - 返回码：`0`

### 9.4 风险状态更新建议

| 项ID | 上轮状态 | 本轮状态 | 说明 |
|---|---|---|---|
| RISK-002 | partial | partial | 本轮继续以质量快照与文档一致性维持交付证据闭环，但工作区冻结分支治理未在本节处理 |
| RISK-008 | open | closed | 完成态事件重复消费已修复；UI 结束态只补未刷新的尾批，研究报告仍保留完整事件集 |
| REV-008 | open | partial | Compare / Research Report 已补最小闭环，DAG 基础多选 / 批量移动 / 批量删除已落地并补齐控制器回归，剩余问题集中在易用性与批量编辑体验 |
| REV-014 | open | partial | compare / research-report 热路径已从 `app.py` 继续下沉，但 UI 仍存在高聚合热点 |

### 9.5 结论

- Phase 4 当前已完成 Compare 报告层与导出层研究化收口，并接入 UI 研究报告导出入口。
- Phase 4 当前未完成项已显式收口：Compare 的 ordered scenarios UI 已完成，DAG 多选 / 批量移动 / 批量删除已落地并补齐控制器回归；剩余 backlog 转为易用性与产品化收口。

## 10. 当前工作区冻结事实（2026-03-07）

### 10.1 已验证

- 当前 `workspace_git_sha=de76ff299bea0557032a40c7117b78e16866d1a1`，且 `artifacts/quality/quality-snapshot.json` 已刷新到 `482 passed / 89.98%`。
- `rtos_sim/ui/app.py` 已降至 `910` 行，文档/表格/启动装配职责已继续下沉到 `window_bootstrap.py`、`document_sync_controller.py`、`table_editor_controller.py`。
- UI 运行完成链路继续保持“流式批次 + 完成态尾批”契约：`SimulationWorker.finished_report` 保留完整 `all_events`，`RunController.on_finished()` 仅补未刷新的尾批事件。

### 10.2 未冻结 / 待后续

- 当前正式 clean freeze 已追平：`review/runtime/i2/clean_freeze/snapshot_meta.json` 固定 `head_commit=de76ff299bea0557032a40c7117b78e16866d1a1`、`dirty_workspace=false`、`freeze_kind=clean_formal`、`tag_name=delivery-baseline-20260308-105134`。
- 当前本地 formal clean freeze 已与 checkpoint commit 对齐；后续若要升级为新的远端主线交付，还需评估远端同步与最终 CI 复核。
- 历史 `review/runtime/i2/baseline_freeze/*` 继续保留为 dirty 证据归档；高级 DAG backlog 已基本完成，剩余治理集中在 formal freeze 与最终 CI 复核。


## 11. 2026-03-07（治理收口 + Compare N-way UI + AGENTS 分层）增量执行记录

### 11.1 目标

- 将 `run` / `analyze-wcrt` / `export-os-config` 的 `--plan-json` 路径改为默认 strict，并收口显式放行口径。
- 将历史 dirty freeze 与正式 clean freeze 明确分流，补齐正式 gate 脚手架。
- 将 FR-13 Compare 从固定双槽切到 ordered scenarios UI，完成装载 / 排序 / 构建 / 导出闭环。
- 将仓库 AGENTS 规则重写为根规则 + `docs/` / `rtos_sim/ui/` / `review/` 分层规则。

### 11.2 实施与证据

- CLI strict 默认化：
  - `rtos_sim/cli/parser_builder.py`
  - `rtos_sim/cli/handlers_planning.py`
  - `rtos_sim/cli/main.py`
  - `review/scripts/i1_ci_gate.sh`
  - `review/scripts/strict_plan_pipeline.sh`
- clean freeze 收口：
  - `review/scripts/i2_freeze_delivery_baseline.sh`
  - `review/scripts/i2_clean_freeze_gate.sh`
  - `tests/test_review_freeze_scripts.py`
- Compare N-way UI：
  - `rtos_sim/ui/panel_state.py`
  - `rtos_sim/ui/panel_builders.py`
  - `rtos_sim/ui/controllers/compare_controller.py`
  - `rtos_sim/ui/app.py`
  - `tests/ui/test_compare_controller.py`
  - `tests/test_ui_compare_io.py`
- AGENTS 分层：
  - `AGENTS.md`
  - `docs/AGENTS.md`
  - `rtos_sim/ui/AGENTS.md`
  - `review/AGENTS.md`

### 11.3 验收命令与结果

1. `python -m pytest -q tests/test_cli_planning_commands.py tests/test_ci_strict_plan_match_gate.py tests/test_review_freeze_scripts.py`
   - 返回码：`0`
2. `python -m pytest -q tests/test_compare.py tests/test_ui_compare_io.py tests/ui/test_compare_controller.py`
   - 返回码：`0`
3. 正式 clean freeze gate 已执行：`bash review/scripts/i2_clean_freeze_gate.sh artifacts/quality/clean_freeze_gate review/runtime/i2/clean_freeze`
   - 返回码：`0`
   - 正式冻结点：`review/runtime/i2/clean_freeze/snapshot_meta.json`（`head_commit=8ba1c0fce2f3ab6d077f145b81209007ceaee0cf`，`tag_name=delivery-baseline-20260308-052817`）

### 11.4 风险状态更新建议

| 风险ID | 上轮状态 | 本轮状态 | 说明 |
|---|---|---|---|
| RISK-002 | partial | closed | clean freeze gate 已执行成功，正式冻结点已替换历史 dirty freeze 成为当前交付基线 |
| RISK-003 | partial | closed | `--plan-json` 默认 strict 已落地，仅 `--allow-plan-mismatch` 可显式告警后继续 |
| REV-008 | open | open | Compare UI 已闭环，剩余问题集中到 DAG 深交互 |
| REV-014 | partial | partial | Compare 热点继续从 `app.py` 下沉，但 UI 高聚合热点仍需后续治理 |

## 12. 2026-03-08（a982185 正式基线升级 + 生命周期/契约收口）增量执行记录

### 12.1 目标

- 将当前 `HEAD=8ba1c0fce2f3ab6d077f145b81209007ceaee0cf` 升级为新的唯一正式 clean freeze 基线。
- 收口 UI 生命周期高风险点：关闭窗口显式 teardown、reset 不再丢失仍在停止中的 worker、无配置启动不再记录伪错误、结束态不再重复常驻整份流式事件。
- 收口 CLI `compare` 的异常边界，统一失败退出码与错误文案，并避免“JSON 成功 / CSV 失败”后的伪成功。
- 将 `scripts/benchmark_sched_rate.py` 的顶层 `gate_metric/gate_pass` 统一到 `candidate_only_uplift`，保留 `macro_uplift` 仅作兼容历史字段。

### 12.2 实施与证据

- 正式基线升级：
  - `artifacts/quality/quality-snapshot.json`
  - `review/runtime/i2/clean_freeze/snapshot_meta.json`
  - `review/runtime/i2/clean_freeze/tag.txt`
- UI 生命周期收口：
  - `rtos_sim/ui/controllers/run_controller.py`
  - `rtos_sim/ui/controllers/form_controller.py`
  - `rtos_sim/ui/app.py`
  - `rtos_sim/ui/panel_builders.py`
  - `tests/ui/test_run_controller.py`
  - `tests/ui/test_form_controller.py`
  - `tests/test_ui_gantt.py`
- CLI compare 失败路径收口：
  - `rtos_sim/cli/main.py`
  - `tests/test_cli_error_paths.py`
- benchmark 顶层契约统一：
  - `scripts/benchmark_sched_rate.py`
  - `tests/test_sched_rate_benchmark_script.py`
  - `review/01-Docx条款追踪矩阵.md`
  - `review/02-审查总报告.md`

### 12.3 验收命令与结果

1. `bash review/scripts/i2_clean_freeze_gate.sh artifacts/quality/clean_freeze_gate review/runtime/i2/clean_freeze`
   - 返回码：`0`
   - 正式冻结点：`review/runtime/i2/clean_freeze/snapshot_meta.json`（`head_commit=8ba1c0fce2f3ab6d077f145b81209007ceaee0cf`，`tag_name=delivery-baseline-20260308-052817`）
2. `python -m pytest -q tests/ui/test_form_controller.py tests/ui/test_run_controller.py tests/test_cli_error_paths.py tests/test_sched_rate_benchmark_script.py tests/test_ui_gantt.py`
   - 返回码：`0`
3. `python -m pytest -q`
   - 返回码：`0`
4. `python scripts/check_doc_baseline_consistency.py --snapshot artifacts/quality/quality-snapshot.json --docs-root docs`
   - 返回码：`0`
5. `python scripts/check_doc_reference_integrity.py --repo-root .`
   - 返回码：`0`
6. `python scripts/benchmark_sched_rate.py --output-dir artifacts/tmp/sched-rate-contract --cases-per-tier 1 --seed 20260304 --target-uplift 0.30 --strict`
   - 返回码：`0`
   - 顶层字段：`gate_metric=candidate_only_uplift`、`gate_value=1.75`、`gate_pass=true`

### 12.4 结果摘要

- `a982185` 已升级为新的正式 clean freeze 基线，旧 `4ae4daa` 不再代表当前对外交付冻结点。
- G-04 当前口径已统一为冻结样本集上的 `candidate_only_uplift >= 0.30`；`macro_uplift` 仅保留为兼容展示字段。
- UI 关闭 / reset 不再出现“UI 已忘记 worker、线程仍可能回调”的直接悬空状态；启动空白编辑器也不再向日志写入伪错误。
- `compare` 失败路径已统一返回失败，不再在构建或写出异常时输出成功语义。


## 13. 2026-03-08（app.py 第二段深拆 + 最终质量快照刷新）增量执行记录

### 13.1 目标

- 将 `rtos_sim/ui/app.py` 继续压缩为主窗口壳层，完成 document/form sync、table editing、window bootstrap 的职责下沉。
- 在不改变 CLI/YAML/导出公开契约的前提下，刷新当前 `HEAD=aa5868e39b69dcd44a8f4ff3f212d50fdb489aba` 的质量快照。
- 为最终 formal clean freeze 与 Windows CI 复核做最后治理准备。

### 13.2 实施与证据

- UI 壳化深拆：
  - `rtos_sim/ui/app.py`
  - `rtos_sim/ui/window_bootstrap.py`
  - `rtos_sim/ui/controllers/document_sync_controller.py`
  - `rtos_sim/ui/controllers/table_editor_controller.py`
  - `tests/ui/test_document_sync_controller.py`
  - `tests/ui/test_table_editor_controller.py`
- 质量快照刷新：
  - `artifacts/quality/quality-snapshot.json`
  - `artifacts/quality/coverage.json`

### 13.3 验收命令与结果

1. `python -m ruff check .`
   - 返回码：`0`
2. `QT_QPA_PLATFORM=offscreen python -m pytest -q`
   - 返回码：`0`（`482 passed`）
3. `python -m pytest --cov=rtos_sim --cov-report=json:artifacts/quality/coverage.json -q`
   - 返回码：`0`
4. `python scripts/quality_snapshot.py --output artifacts/quality/quality-snapshot.json --coverage-json artifacts/quality/coverage.json`
   - 返回码：`0`
   - 质量快照：`evidence_git_sha=aa5868e39b69dcd44a8f4ff3f212d50fdb489aba`，`pytest=482 passed`，`coverage=89.98%`

### 13.4 当前状态摘要

- `rtos_sim/ui/app.py` 已降至 `<1000` 行目标内，主入口热点显著收缩。
- 当前质量快照已提升到 `482 passed / 89.98%`，已高于既有治理门槛。
- 正式 clean freeze 与 GitHub Actions Windows 绿灯确认已在后续治理归档步骤中闭环。


## 14. 2026-03-08（formal clean freeze 归档提交）增量执行记录

### 14.1 目标

- 将 `1069a1f577f3b9a5f5f945fbb3eac554c73b10f7` 的正式 clean freeze 证据归档到仓库。
- 将 `docs/26-交付清单.md` 与本执行记录中的 freeze 状态改为“已完成归档”。
- 确认远端 `CI` 主工作流对本次 push 的 `Test` 矩阵已全部完成。

### 14.2 实施与证据

- 代码/文档治理提交：`1069a1f577f3b9a5f5f945fbb3eac554c73b10f7`
- formal freeze 目录：`review/runtime/i2/clean_freeze`
- formal freeze 元数据：`review/runtime/i2/clean_freeze/snapshot_meta.json`
- formal freeze 标签：`delivery-baseline-20260308-081706`
- 远端 workflow：`https://github.com/laoyu17/shenrengongju/actions/runs/22817186056`

### 14.3 验收命令与结果

1. `git push origin main`
   - 返回码：`0`
   - 远端分支：`origin/main` 已推进到 `1069a1f577f3b9a5f5f945fbb3eac554c73b10f7`
2. `bash review/scripts/i2_clean_freeze_gate.sh artifacts/quality/clean_freeze_gate review/runtime/i2/clean_freeze`
   - 返回码：`0`
   - 正式冻结点：`review/runtime/i2/clean_freeze/snapshot_meta.json`（`head_commit=1069a1f577f3b9a5f5f945fbb3eac554c73b10f7`，`tag_name=delivery-baseline-20260308-081706`）
3. `git push origin delivery-baseline-20260308-081706`
   - 返回码：`0`
4. 远端 `CI` run 页面复核：`https://github.com/laoyu17/shenrengongju/actions/runs/22817186056`
   - 结果：`Status=Success`
   - `Matrix: test` 显示 `2 jobs completed`；结合 `.github/workflows/ci.yml` 中 `os: [ubuntu-latest, windows-latest]`，可确认 Windows 矩阵已完成并转绿

### 14.4 结果摘要

- `1069a1f577f3b9a5f5f945fbb3eac554c73b10f7` 已完成新的正式 clean freeze 归档，旧 `delivery-baseline-20260308-052817` 不再代表当前正式交付基线。
- `review/runtime/i2/clean_freeze/*`、正式 tag 与远端 `CI` 结果已闭环，可作为当前对外交付的正式证据链。
- 本次归档提交只补充 freeze 证据与状态文案，不改变已冻结代码行为。


## 15. 2026-03-08（checkpoint commit + 本地 formal clean freeze 追平）增量执行记录

### 15.1 目标

- 以当前收口工作区创建本地 checkpoint commit，作为新的 freeze 锚点。
- 在 clean workspace 上重跑 `review/scripts/i2_clean_freeze_gate.sh`，将 `review/runtime/i2/clean_freeze/*` 追平到本轮结果。
- 刷新根质量快照、research 产物与主线 docs/review 的当前口径，但不改写历史 freeze 归档段落。

### 15.2 实施与证据

- checkpoint commit：`de76ff299bea0557032a40c7117b78e16866d1a1`（`chore: checkpoint before formal clean freeze refresh`）
- formal freeze 目录：`review/runtime/i2/clean_freeze`
- formal freeze 元数据：`review/runtime/i2/clean_freeze/snapshot_meta.json`
- formal freeze 标签：`delivery-baseline-20260308-105134`
- 质量快照：`artifacts/quality/quality-snapshot.json`（`evidence_git_sha=de76ff299bea0557032a40c7117b78e16866d1a1`，`pytest=482 passed`，`coverage=89.98%`）

### 15.3 验收命令与结果

1. `git commit -m "chore: checkpoint before formal clean freeze refresh"`
   - 返回码：`0`
   - 新提交：`de76ff299bea0557032a40c7117b78e16866d1a1`
2. `bash review/scripts/i2_clean_freeze_gate.sh artifacts/quality/clean_freeze_gate review/runtime/i2/clean_freeze`
   - 返回码：`0`
   - 正式冻结点：`review/runtime/i2/clean_freeze/snapshot_meta.json`（`head_commit=de76ff299bea0557032a40c7117b78e16866d1a1`，`tag_name=delivery-baseline-20260308-105134`）
3. `python -m pytest -q`
   - 返回码：`0`（`482 passed`）
4. `python scripts/quality_snapshot.py --output artifacts/quality/quality-snapshot.json --coverage-json artifacts/quality/coverage.json`
   - 返回码：`0`
5. `python scripts/check_doc_baseline_consistency.py --snapshot artifacts/quality/quality-snapshot.json --docs-root docs`
   - 返回码：`0`
6. `python scripts/check_doc_reference_integrity.py --repo-root .`
   - 返回码：`0`

### 15.4 结果摘要

- 本地正式 clean freeze 已从 `1069a1f577f3b9a5f5f945fbb3eac554c73b10f7` / `delivery-baseline-20260308-081706` 追平到 `de76ff299bea0557032a40c7117b78e16866d1a1` / `delivery-baseline-20260308-105134`。
- 历史 section 11~14 继续保留原时间点事实；本轮只更新“当前事实源”与新增一节本地追平记录。
- 当前工作区剩余为 freeze 产物与 docs/review 追平后的未提交改动；如需把这轮结果继续推进为新的主线交付，还需单独决定是否提交这些证据文件并做远端同步。
